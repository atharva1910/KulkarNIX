[build]
target = "x86_64-unknown-none"

[target.x86_64-unknown-none]
rustflags = [
    "-C", "link-arg=-Tlinker.ld", # Pass custom linker script to Rust
    "-C", "relocation-model=static",
    "-C", "code-model=large",     # Ensure memory addresses fit kernel space
]