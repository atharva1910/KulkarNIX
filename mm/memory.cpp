#include "paging.h"

// We have one page directory and the page directory points to the "pages"
void paging_init()
{
    // init page directoty
    uint32_t page_table[1024] = {0};

}

static inline init_page_dir()
{
    
}
